# 将棋AIの履歴取込み（2026-09-22）

ユーザーからGitHubへの読み書きと過去の作業の保存を明示的に許可されたため、`U10-h/shogi-AI` へ送信する履歴を整理した。GitHubのリポジトリは通常のGit接続で読み取れ、取得時点では空だった。書き込みはまだ成功していない。

## 保存した範囲と順序

Webアプリの17コミット（`003d6a0`〜`5f6e8ba`）を、ハッシュ・親子関係・日時を変更せず維持した。その後、実際に保存されていた探索研究ZIPを次の順に取り込んだ。

| 取込みコミット | 保存版 | 含まれる主な追加内容 |
|---|---|---|
| `e4586f2` | v0.6 | v0.5の探索手法・文献調査、v0.6の対やねうら王対局・診断 |
| `00b712c` | v0.8 | v0.7の評価実験、v0.8のNNUE導入・差分計算 |
| `e7bfdc3` | v0.10 | v0.9の探索改善、v0.10の評価学習・モデル・対局 |
| `4173273` | v0.11 | 単純補正との対照実験、探索打切りの改善 |
| `dc2c19b` | v0.12 | 候補間の評価差学習、探索の葉のトレース |
| `4ef67a2` | v0.14 | v0.13の学習した枝刈り、v0.14の読み順・駒取り履歴・LMR比較 |

各行は実在する完成済みZIPの取込みであり、失われた過去の開発コミットを推測して復元したものではない。取込みコミットの日付は今回の保存日。各実験の実施条件や結果は元の報告書に記載されている。v0.1〜v0.14の報告書と残存チェックポイントを保持した。

探索研究の保存先は `engines/shogi-search-lab/`。最新完成版には5,266ファイルがあり、うち5,265ファイルが元の `MANIFEST.json` に記録されている。元のZIPと展開後の双方でSHA-256が全件一致した。実験の `.log` も含めた。コンパイル結果やキャッシュを新たに追加していない。

ZIPのSHA-256、ファイル数、取込み前コミットは [search-lab-imports.json](search-lab-imports.json) に記録した。既存のWeb画面と同梱された観戦用データは元の版を保持している。探索研究の更新をWebアプリのエンジンへ自動的に置き換えたわけではない。

作業中のv0.15は `work/v0.15-checkpoint-20260922` に分離する。未完成の結果をv0.14の完了実験として扱わない。そのブランチには取得した時点と未完了範囲を別途記録する。

## 今回の確認

- `npm run check`：構文・ローカル参照・同梱エンジンの整合性に合格。
- `npm test`：59件合格、失敗0件。
- `npm run test:engine`：実NNUEの初期化、合法な着手と読み筋、MultiPV、停止・再探索、7手以上の解説手順、背景解析との同時実行に合格。
- `make -j2 test`（探索研究v0.14）：基本・セッション・advancedの既存自己テストに合格。
- 元のWebアプリの先頭17コミットが現在の履歴の祖先であることを確認。

今回の目的は履歴保存であり、過去の対局・学習・性能測定は再実行していない。上記テストは保存後のソースの動作確認であり、棋力向上を追加で示すものではない。

## リモート送信の状態

**未完了。** GitHub連携APIはHTTP 400 `Invalid MCP request metadata` で失敗した。通常のGitではリポジトリを取得できたが、`git push --dry-run origin main` は書き込み用の認証がなく `could not read Username` で失敗した。リモートブランチを変更する操作は成功していない。

接続が復旧したら、まずリモートの最新状態を再取得して、その後の変更がないかを確認する。空のままなら `main` と作業ブランチを通常のpushで送信する。リモートに新しい内容があれば差分を確認して統合し、強制pushはしない。最後にリモートのコミットSHAとローカルを照合し、この保存状況の記述を更新する。ユーザーの今回の許可を引き継ぐため、同じ書き込みについて再承認を求める必要はない。
