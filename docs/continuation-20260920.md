# 2026-09-20 継続改善：読み筋と解説の一致

前回の5回・15サイクルを完了した状態から、未完了の解説検証とGitHub保存を確認した。今回は探索設定・WASM・NNUE・Qwenの重みを変更していない。

## 修正

- 根拠にない「同銀」、単位なし評価値、点数の符号反転、勝率の創作を検出。
- 矢印で示す手順の逆転・別の枝の継ぎ合わせ、何手目かの不一致を引用された手順に照合。
- 「未確認ですが、必至です」のような留保と断定の混在を検出。用語の説明は盤面の証拠にしない。
- 比較・最善手・方針の質問では、両候補の7手順を講評より先に渡す。手順の型を保持し、駒の収支を形勢評価へすり替える例を検出。
- 中止した回答を直ちに破棄し、次の生成は前の生成の終了まで待つ。古いモデルの初期化失敗で新しいモデルが停止する競合も修正。
- 探索・用語・対話モジュールをオフライン用事前キャッシュに追加。

## 検証

全59件の自動テスト成功。構文・ローカル参照・エンジンバイナリの整合性も確認した。

| 人手で作成した回答例の検査 | 前回 | 修正後 |
|---|---:|---:|
| 誤答を通した数 | 12 / 14 | 0 / 14 |
| 許容回答を拒否した数 | 0 / 8 | 0 / 8 |
| 期待どおりの判定 | 10 / 22 | 22 / 22 |

これは修正対象を含む回帰検証であり、未見データへの一般化性能やQwenの生成品質ではない。自然言語の全主張を証明する仕組みではなく、特定の表記・断定・数値・手順に対する保守的な検査。意味の取り違えをすべて検出することはできない。不合格時は既存の根拠付き解説に戻る。

実WASMエンジンの固定5局面では、各3展開、計15展開がすべて合法・9手以上。候補3〜4手、応手3通り、再問い合わせの追加探索は全5局面で0回。基準180msの短時間設定で、背景解析から手順取得まで2.016〜2.190秒だった。これは開発環境の参考値で、実機速度ではない。

既存の長時間参照手との一致は今回2/5。前回の最終測定4/5から揺れており、棋力向上は確認していない。探索コードは変更していないが、短い時間制限の結果は環境や負荷にも依存する。同じ自生成棋譜からの5局面であり、独立した戦術問題集・実戦対局の評価を代替しない。

## 再現

```sh
npm test
npm run check
node scripts/check-tutor.mjs --baseline cd38a5fdbd2f1d9f8f321cb5a3608be5dc657e7c
node scripts/benchmark.mjs continuation-20260920 --all
```

比較にはGit履歴が必要。ソースZIPのみでは `node scripts/check-tutor.mjs` で現行版を確認できる。結果・事例・探索履歴は `docs/experiments/` の `tutor-reliability.json`、`tutor-regression-cases.json`、`continuation-20260920.json`。

## 残課題

- POCO F6 Pro実機でのQwen生成、日本語解説の有用性、連続対局時の速度と発熱。
- 静かな中飛車の局面、無理攻め・受けについて、独立した局面と反証を集めること。
- GitHub保存。2026-09-20も `U10-h/shogi-AI` への連携の書き込みが `403 Resource not accessible by integration` で拒否された。利用ページのソースZIPは更新するが、GitHub保存は未完了。
- 評価関数単独の利用条件は、前回確認できた出所以外の新しい裏付けを得ていない。
