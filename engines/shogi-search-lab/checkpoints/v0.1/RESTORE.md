# 第1回の状態を再現する

ここには第1回の自作ソース、Makefile、研究記録、測定結果を保存しています。
再利用した第三者ソースは今回と共通で、プロジェクト直下の `vendor/` を使用します。

別ディレクトリへこの `src/` と `Makefile`、共通の `vendor/` をコピーし、そこで `make` を実行すれば、第1回の探索をビルドできます。
現在のソースへ上書きする必要はありません。

`results/environment.json` のソースハッシュと、この `src/` の内容は一致します。測定時間は元の環境での値です。
