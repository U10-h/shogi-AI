# v0.8 before search-core optimization

This source snapshot was saved before the v0.9 edits. From the project root:

```bash
python3 scripts/build_checkpoint.py v0.8
```

The output is `build/shogi-lab-v0.8`. It links the unchanged vendored rules.
The fixed external NNUE model is obtained with `scripts/fetch_opponent.py`.
