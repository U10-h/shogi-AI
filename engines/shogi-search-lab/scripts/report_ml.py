#!/usr/bin/env python3
"""Write the Japanese experiment report from final audited metrics."""
import json
from pathlib import Path
ROOT=Path(__file__).resolve().parents[1];D=ROOT/'results/v0.10'
s=json.loads((D/'summary.json').read_text());m=s['matches'];a=s['staticTest'];q=s['test']
f=lambda x:f'{x:,.2f}'
match_text=f"{m['wins']}勝{m['losses']}敗・引分{m['draws']}・未決着{m['unresolved']}"
interval='未決着を含むため、全局の得点率は推定しません。'
if 'scoreRate' in m:
 lo,hi=m['pairedOpeningBootstrap95'];interval=f"得点率{m['scoreRate']:.1%}、開始局面の先後ペア単位で再標本化した95%区間は{lo:.1%}〜{hi:.1%}。16開始局面の範囲内の参考値で、一般的な勝率やEloではありません。"
test_rows=[]
for ms in ['1000','3000']:
 r=q[ms];b=r['models']['base'];c=r['models']['anchored50'];lo,hi=c['pairedGameBootstrap95']
 test_rows.append(f"| {int(ms)//1000}秒 | {r['roots']} | {f(b['meanGapCp'])} | {f(c['meanGapCp'])} | {c['better']} / {c['equal']} / {c['worse']} | {f(c['meanPairedDeltaCp'])} [{f(lo)}, {f(hi)}] |")
report=f'''# 将棋AI：評価関数の学習・大規模化・観察・再構築（v0.10）

2026-09-21。元のNNUEを基盤に機械学習モデルを実装し、192の対局進行から56,805局面を生成しました。最初の直接回帰で探索品質が悪化したため、元の評価を守る制約付きの学習へ作り直し、選択後に固定したモデルで最終評価を行いました。

最終候補は、未使用の非王手8,827局面で平均絶対誤差が{f(a['base']['maeCp'])}→{f(a['candidate']['maeCp'])}cp（2.45%減）。旧評価との対局は**{match_text}**でした。一方、1秒の96局面では改善5・同等87・悪化4、3秒の24局面では改善1・同等23・悪化0で、探索品質の改善は少数局面に集中しています。局所的な改善候補として残し、未知戦型・長時間対局での棋力向上は未確定とします。

## 改善した点

1. **NNUEヘッドを追加学習。** 凍結した特徴変換に、32→32→1の非線形ヘッドを接続。1,089パラメータをNumPyの逆伝播・Adamで追加学習しました。量子化した整数推論でエポックを選び、C++でそのまま使用できます。
2. **実験を拡大し、分割と重複を管理。** 192進行、21,889回の教師探索、56,805局面。教師生成と固定ノード実験は4並列、時間比較は直列。学習・調整・最終評価は対局単位で分割し、同一局面・左右反転の分割間重複を除きました。
3. **評価値の一致だけで採用しない。** 静的誤差、探索後の選択手、単純補正の対照実験、先後交換の32対局を別々に評価しました。
4. **失敗を観測して目的関数を再構築。** 教師との差を制限し、Huber損失と元パラメータへの正則化で過大な補正を抑えました。
5. **測定と再開を改善。** 教師の不成候補の欠落を修正し、探索出力を採点前に保存。全棋譜・読み筋を別実装でも検証しました。

## 学習データとモデル

| 分割 | 対局進行 | 重複除去後の局面 | 学習・静的評価に使う非王手局面 |
|---|---:|---:|---:|
| 学習 | 128 | 37,887 | 34,774 |
| 調整 | 32 | 9,275 | 8,576 |
| 最終評価 | 32 | 9,643 | 8,827 |

教師は固定版YaneuraOu NNUE KP256 6.03。深さ6・MultiPV3の上位候補から乱数種を固定して進行を作り、根と子局面の探索評価を教師値にしました。各進行は最大120手で、59進行のみ終端に到達しています。192の完局棋譜や192局の勝敗学習という意味ではありません。序盤12手未満、詰み、絶対値8,000cp以上のラベルを除き、王手中の局面は学習対象外としました。

基盤の454,432パラメータは凍結。学習対象は後段の1,089パラメータのみで、ネットワーク全体の新規学習ではありません。採用候補では整数パラメータ77個が元と変化しました。全モデルの由来・ハッシュは `models/v0.10/MODEL_CARD.md` に記録しています。

16種類の開始手順は分割間で共通です。重複局面は除いていますが、類似局面や戦型は共有しており、未知戦型への汎化を保証しません。教師も基盤も同じNNUEなので、教師に近づいたことと独立した棋力向上は区別します。

## 第1段階：直接回帰は静的誤差を減らし、探索を悪化させた

最初は教師の探索値へ直接MSE回帰しました。60エポック、バッチ256、学習率0.0003、検証値が最小の25エポックを選択。調整用の静的MAEは731.06→664.54cpに改善しました。

しかし、2モデルの候補を同時採点した32局面・各20,000ノードの比較では、教師の最良候補との差が131.59→158.47cpに悪化しました。元の誤差が100cp以内だった1,682局面に限ると、誤差が47.85→245.12cpに増大。平均絶対補正449.15cp、最大2,124.44cpで、元の有用な評価を大きく崩していました。

観察すると、教師値と元評価の差は調整集合で平均+452.64cp、直接回帰の出力変化は平均+429.48cpでした。根と子、候補順位で教師との差も異なります。サンプル構成と探索の深さに由来する傾向を、そのまま手番側の楽観的な評価として吸収した可能性があります。これは観察に基づく解釈で、原因の完全な切り分けではありません。

## 第2段階：補正を制限して再構築

新しい教師値を、cp単位で次のように作りました。

`new_target = base + α × clip(teacher − base, −200, +200)`

αを0.25と0.50で比較。Huber損失の折点は約166.7cp、元パラメータへの正則化係数0.02、学習率0.00015。60エポックのうち、整数推論の検証MAEが最小の55エポックを使用しました。これは補正教師値の制限であり、最終ネットワーク出力に同じ上限を強制する処理ではありません。

| モデル | 調整用の静的MAE（cp） | 探索後の平均候補差（cp） | 判断 |
|---|---:|---:|---|
| 元評価 | 731.06 | 131.59 | 基準 |
| 直接回帰 | 664.54 | 161.56 | 不採用 |
| 補正25% | 722.13 | 131.72 | 改善を確認できず |
| 補正50% | 715.43 | 81.50 | 最終評価候補 |

この表の探索値は**4モデル共通の候補集合**で再採点した結果です。第1段階の2モデル比較とは候補集合が異なるため、直接回帰の158.47と161.56を同じ条件の値として扱いません。

補正50%の平均絶対補正は42.53cp、最大101.11cp。元の誤差100cp以内の集合でも、新誤差54.36cpに抑えられました。ただし、多くの変化が手番側への約+40cpに集まったため、出力バイアスだけを+40cpにする対照モデルも作りました。

3モデル共通の調整用比較では、元139.50、単純補正114.88、学習補正89.94cp。学習補正は元に対して2改善・29同等・1悪化、単純補正に対して1改善・30同等・1悪化でした。この差は少数の局面によるため、複雑な将棋知識の獲得と断定しません。

## 固定後の最終評価

モデルを2026-09-21T14:18:44Zにハッシュで固定。その後のテストで重みを再調整していません。探索のアルゴリズムはv0.9と共通のPVS＋静止探索。変更したのは評価ヘッドです。

### 静的評価

| 指標 | 元評価 | 再構築版 |
|---|---:|---:|
| MAE（cp） | {f(a['base']['maeCp'])} | {f(a['candidate']['maeCp'])} |
| RMSE（cp） | {f(a['base']['rmseCp'])} | {f(a['candidate']['rmseCp'])} |
| 絶対誤差95%点（cp） | {f(a['base']['p95AbsCp'])} | {f(a['candidate']['p95AbsCp'])} |

40手未満、40〜79手、80手以降の各群でMAEは低下しました。ただし、これらの局面は同一進行内で相関があり、8,827個の独立な実験とは数えません。

### 探索後の選択手

両モデルの手と教師自身の手を候補集合にし、教師の深さ10で同時採点。候補差はその集合の最高値からの差で、小さいほど良い指標です。全合法手の厳密な最善手との差ではありません。最終測定では両モデルとも最初の反復を完了しており、代替着手は0件でした。

| 時間/手 | 局面数 | 元の平均候補差 | 再構築版 | 改善 / 同等 / 悪化 | 再構築−元の平均差と95%区間（cp） |
|---|---:|---:|---:|---|---|
{chr(10).join(test_rows)}

区間は対局進行単位で5,000回ブートストラップした参考値です。どちらも0を含み、強さの向上を確定するものではありません。3秒の24局面は1秒集合の一部で、独立な120局面ではありません。

1秒集合の1局面で、教師が双方共通の手に-31,111cpを返しました。固定ソースの優越・劣等反復スコア28000rawに対応する極端値で、普通の駒得差と同じ意味ではありません。全件の集計は保持し、全候補が絶対値8,000cp未満の95局面を補助的に集計すると、平均候補差は135.00→126.35cpでした。除外は重み選択に使っていません。

目立つ改善はgame180・24手目。元の `7g7f` に対し再構築版は `3f3e` を選び、教師候補差が1,614→0cpでした。1秒・3秒とも同じ局面で、3秒条件の平均改善はこの1局面だけによります。悪化例はgame170・98手目で、`3i3h` から `3i2h` に変わり、候補差0→941cp。両者とも完成深さ3でした。良い例だけを選ばず、全120測定をCSVに収録しました。

### 旧評価との32対局

学習に使っていない16開始局面で先後交換。両者12,000ノード/手、最大200手、同じ探索本体で対局しました。

**{match_text}。** {interval}

実対局の着手数は{m['plies']:,}、検証した読み筋の手数は{m['pvMoves']:,}。初回反復を完了できず最初の合法手を選んだ回数は元{m['fallbacks']['base']}、再構築版{m['fallbacks']['anchored50']}でした。この代替処理も対局条件の一部であり、純粋な評価値だけの影響としては切り分けていません。全32局のKIF、着手前後局面、探索結果を同梱しています。

## 検証と測定上の修正

- 既存回帰テスト6,532件が成功。
- 独立Python特徴抽出1,024局面、元モデルのPython/C++整数評価56,805局面が一致。
- 学習3モデルそれぞれ1,024局面でC++の差分・全再計算・検証モードとPython評価が一致。各8局面の全再計算/差分探索も一致。元モデルの8探索はv0.9とスコア・PV・ノード・停止理由が一致。
- 数値微分による勾配確認の最大誤差は約1.46×10⁻¹²。
- 全選択手・PV・終端・反復・KIFの往復をtsshogiで検証。
- データ生成の進行保存で21件の最新書き込みが残らなかったため、完了出力を検査し、同じ種で該当進行を再生成。正本は追記後に変更しない `games-final` へ統一しました。
- 最終測定の30件目で、教師の既定設定が合法な不成 `7b9d` を候補から落としていることを検出。最終採点の全件を `GenerateAllLegalMoves=true` に統一しました。既測定29件は自作探索の出力を再利用し、失敗した1件だけ最初の診断再実行を使用。以降は採点前に探索出力を保存しています。修正履歴と旧採点も残しています。

## 考察と次の改善

今回の結果は、教師値への回帰誤差を最小にすることだけでは、探索の選択手が良くなるとは限らないことを示します。特に静止探索の葉は深さや手番が揃わないため、手番側への定数補正でも根の着手が変わり得ます。直接回帰が壊した既存評価を、補正制限と正則化で守る方が有効でした。

次の優先課題は、(1)自作探索で訪れた葉局面を学習分布に加える、(2)同じ根の候補順位を直接学習し、元から良い評価を保持する損失と併用する、(3)第1反復未完了時の代替着手を改善し、評価関数との効果を分ける、(4)未知の開始手順・別教師・長い持ち時間で対局数を増やすことです。これらは今回の固定テストには後付けしていません。新しい学習には別の未観測評価集合を用意する必要があります。

再構築版は `--eval-model build/models/v0.10/anchored50.nnue` で利用できます。元モデルも比較用に維持します。新規フルネットワーク、数百万局面学習、本家やねうら王を上回る棋力、Elo向上のいずれも本実験からは主張しません。

![学習曲線と最終評価](results/v0.10/learning-evaluation.png)

再現コマンドは `REPRODUCE-v0.10.md`、モデルの詳細は `models/v0.10/MODEL_CARD.md`。元データ、モデル、全比較、棋譜、測定修正の記録はプロジェクト一式に収録しています。
'''
(ROOT/'REPORT-v0.10.md').write_text(report)
print(ROOT/'REPORT-v0.10.md')
